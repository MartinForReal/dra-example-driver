---
apiVersion: {{ include "dra-infiniband-driver.resourceApiVersion" . }}
kind: DeviceClass
metadata:
  name: {{ include "dra-infiniband-driver.driverName" . }}
spec:
  selectors:
  - cel:
      expression: "device.driver == '{{ include "dra-infiniband-driver.driverName" . }}'"
