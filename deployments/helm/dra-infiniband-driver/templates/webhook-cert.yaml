{{- if .Values.webhook.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "dra-infiniband-driver.fullname" . }}-webhook-cert
  namespace: {{ include "dra-infiniband-driver.namespace" . }}
  labels:
    {{- include "dra-infiniband-driver.labels" . | nindent 4 }}
    app.kubernetes.io/component: webhook
spec:
  dnsNames:
    {{- $svcName := printf "%s-webhook" (include "dra-infiniband-driver.fullname" .) }}
    {{- $svcNamespace := (include "dra-infiniband-driver.namespace" .) }}
    - {{ $svcName }}.{{ $svcNamespace }}.svc
  issuerRef:
    kind: Issuer
    name: {{ include "dra-infiniband-driver.fullname" . }}-webhook-issuer
  secretName: {{ include "dra-infiniband-driver.fullname" . }}-webhook-cert
{{- end }}
